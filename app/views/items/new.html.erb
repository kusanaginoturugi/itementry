<% content_for :title, "道具登録" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">道具登録</h1>
  <%= link_to "一覧へ戻る", items_path, class: "btn btn-outline-secondary" %>
</div>

<%= render "form", item: @item %>
