<% content_for :title, "帳票登録" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="h4 mb-0">帳票登録</h1>
  <%= link_to "一覧へ戻る", books_path, class: "btn btn-outline-secondary" %>
</div>

<%= render "form", book: @book %>
